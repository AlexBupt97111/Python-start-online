import random
a = []
for i in range(4):#  4-dimension matrix
    row = []# create empty row
    for j in range(4):
        row.append(random.randint(1,16))
    a.append(row)# add rows while j<=4
for el in a:
    print(el)
s = sum(a[i][j] for i in range(4) for j in range (4))
print(s)
